<view class="classic-cont" wx:for="{{InformationList}}">
    <view bindtap="goClassicDeails" class="classic-item" data-informationid="{{item.id}}">
        <image src="{{item.icon}}"></image>
        <view class="classic-introduction">{{item.title}}</view>
    </view>
</view>
